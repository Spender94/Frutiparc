node_modules/
data/
# La règle `data/` vise le dossier du serveur (scores, sessions…) ; les
# données EXTRAITES du SWF de Kaluga (scripts/extract-kaluga.js) sont du
# code, elles se livrent.
!public/kaluga/data/

# MotionBall 2 daily challenge map — regenerated by the server (boot +
# midnight Paris roll). Tracking it would freeze the map across deploys.
Games/motionBall2/mb2data.dat
